# Woo / 无我笔记 - 软件设计文档

## 文档信息

| 项目   | 内容         |
| ---- | ---------- |
| 产品名称 | Woo / 无我笔记 |
| 文档版本 | v1.0       |
| 编写日期 | 2026-04-23 |
| 文档状态 | 初稿         |

***

## 1. 产品概述与目标用户

### 1.1 产品定位

Woo（无我笔记）是一款跨平台 Markdown 桌面笔记软件，核心定位是：

- **极致简洁的写作环境**，减少干扰，专注创作
- **融合思维导图大纲**，提供结构化思考能力
- **Git 版本管理**，实现安全可靠的数据同步
- **AI 辅助能力**，提升写作效率与质量

### 1.2 目标用户

- **内容创作者**：作家、博主、记者等需要长期专注写作的人群
- **知识工作者**：学生、研究人员、程序员等需要整理笔记和知识的人群
- **思维导图爱好者**：喜欢结构化思考和视觉化表达的用户
- **极简主义者**：追求简洁界面和高效工作流的用户

### 1.3 核心价值主张

1. **极简设计**：完全避免不必要的视觉干扰
2. **双视图协同**：Markdown 编辑与思维导图大纲无缝切换
3. **版本控制**：基于 Git 的可靠数据管理和同步
4. **AI 赋能**：智能写作辅助，提升创作效率

***

## 2. 功能模块划分与说明

### 2.1 功能模块总图

```
Woo / 无我笔记
├── 核心编辑模块
│   ├── Markdown 编辑器
│   ├── 思维导图/大纲视图
│   ├── 双向归类机制
│   ├── 写作辅助功能
│   └── 表格增强
├── 数据与集成模块
│   ├── GitHub 仓库版本管理
│   ├── 第三方图床集成
│   └── AI 插件系统
├── 语音写作模块
│   ├── 基础语音转文字
│   └── 写作级优化转写
└── 界面与交互模块
    ├── 三栏式布局（可选）
    ├── 全局缩放
    └── 主题系统
```

### 2.2 核心编辑模块

#### 2.2.1 Markdown 编辑增强

| 功能             | 说明                     |
| -------------- | ---------------------- |
| 标准 Markdown 支持 | 标题、列表、代码块、引用、表格、链接、图片等 |
| 扩展语法           | 居中对齐、脚注、任务列表、数学公式（可选）  |
| 快捷键行为          | 同一快捷键第二次按下取消当前格式       |
| 多光标编辑          | 支持同时编辑多个位置             |
| 列表自动生成导图       | 基于有序/无序列表结构自动生成思维导图    |

#### 2.2.2 思维导图/大纲视图

- **节点折叠/展开**：支持大纲节点的层级折叠
- **视图同步联动**：编辑区与导图区实时同步
- **拖拽调整层级**：通过拖拽调整节点层级关系

#### 2.2.3 双向归类机制

- **从左到右**：传统层级大纲视图
- **从右到左**：分类标注视图（反向索引、标签分类）

#### 2.2.4 写作辅助功能

- **排版控制**：缩进、对齐方式
- **打字机模式**：固定写作行位于屏幕指定百分比位置
- **全局缩放**：类似 VS Code 的 `Ctrl + / -` 缩放

#### 2.2.5 表格增强

- **文字格式**：加粗、斜体、下划线
- **单元格对齐**：左对齐、居中、右对齐
- **表头定义**：顶部表头、左侧表头
- **自定义函数**：基础计算函数（求和、平均等）

### 2.3 数据与集成模块

#### 2.3.1 GitHub 仓库版本管理

- **账号授权**：OAuth 2.0 授权 GitHub 账号
- **分支映射**：每份笔记对应仓库的一个独立分支
- **同步操作**：commit / push / pull
- **冲突处理**：提供可视化冲突解决界面

#### 2.3.2 第三方图床集成

- **支持的图床**：阿里云 OSS、七牛云、GitHub 等
- **自动上传**：粘贴图片时自动上传并转换为 Markdown 链接

#### 2.3.3 AI 插件系统

- **Chat 面板**：参考 IntelliJ IDEA / VS Code 插件风格
- **上下文感知**：基于当前笔记内容提问
- **插件化架构**：支持接入不同 AI 服务提供商

### 2.4 语音写作模块

- **第一阶段**：基础语音转文字
- **第二阶段**：写作级优化转写（自动纠错、断句、标点修正）

***

## 3. 技术架构建议

### 3.1 技术栈选择

**前端**：Vue 3 + TypeScript + Electron
**后端**：Java 17 + Spring Cloud
**存储**：Redis + MySQL
**部署**：Docker

**选择理由**：

1. **前端**：Vue3 + TypeScript 提供优秀的开发体验，Electron 确保跨平台支持
2. **后端**：Java 17 + Spring Cloud 提供稳定可靠的服务端架构
3. **存储**：MySQL 存储结构化数据，Redis 提升缓存性能
4. **部署**：Docker 简化部署流程，提高环境一致性

### 3.2 整体架构设计

```
┌─────────────────────────────────────────────────────────┐
│                     前端层 (Electron)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │Vue3 应用 │  │编辑器组件│  │AI 面板   │  │设置面板 │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
├─────────────────────────────────────────────────────────┤
│                   Electron 桥接层                       │
│              (IPC 通信 + 原生 API 封装)                   │
├─────────────────────────────────────────────────────────┤
│                   后端服务层 (Java)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │Spring Boot│  │Git 集成  │  │图床服务  │  │AI 服务  │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
├─────────────────────────────────────────────────────────┤
│                   数据存储层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │MySQL     │  │Git 仓库  │  │对象存储  │  │Redis    │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 3.3 前端技术栈推荐

- **框架**：Vue 3 + TypeScript
- **状态管理**：Pinia（Vue 3 官方推荐）
- **UI 组件库**：自定义组件（参考 Effie 风格，避免过度设计）
- **Markdown 编辑器**：CodeMirror 6 或 Monaco Editor
- **思维导图**：基于 VueFlow 或 D3.js 自定义实现
- **构建工具**：Vite + Electron Builder
- **IPC 通信**：Electron IPC 模块

### 3.4 后端技术栈（Java 17）

- **核心框架**：Spring Boot 3.2 + Spring Cloud
- **Git 操作**：JGit（Java Git 库）
- **HTTP 客户端**：RestTemplate / WebClient
- **数据库**：MySQL 8.0
- **缓存**：Redis
- **认证**：Spring Security + OAuth2
- **API 文档**：SpringDoc OpenAPI 3
- **部署**：Docker + Kubernetes（可选）

***

## 4. 数据模型与存储设计

### 4.1 本地存储结构

```
Woo Notes/
├── config/
│   ├── settings.json          # 应用设置
│   ├── github_token.json      # GitHub 授权信息（加密存储）
│   └── image_host.json        # 图床配置
├── data/
│   ├── notes.db               # SQLite 数据库（笔记元数据）
│   └── cache/                 # 缓存目录
└── repositories/              # Git 仓库目录
    └── [username]/
        └── [repo-name]/       # 每个 GitHub 仓库对应一个目录
            ├── note-1/        # 笔记 1（分支：note/note-1）
            └── note-2/        # 笔记 2（分支：note/note-2）
```

### 4.2 数据模型

#### 4.2.1 笔记模型 (Note)

```typescript
interface Note {
  id: string;                    // 唯一标识符（UUID）
  title: string;                 // 笔记标题
  content: string;               // Markdown 内容
  createdAt: Date;               // 创建时间
  updatedAt: Date;               // 更新时间
  repositoryId: string;          // 关联的仓库 ID
  branchName: string;            // Git 分支名
  tags: string[];                // 标签列表
  metadata: {
    wordCount: number;           // 字数统计
    lastSyncAt?: Date;           // 最后同步时间
    syncStatus: 'idle' | 'syncing' | 'conflict';
  };
}
```

#### 4.2.2 仓库模型 (Repository)

```typescript
interface Repository {
  id: string;
  owner: string;                 // GitHub 用户名
  name: string;                  // 仓库名
  localPath: string;             // 本地路径
  defaultBranch: string;         // 默认分支
  isPrivate: boolean;            // 是否私有
  lastSyncAt?: Date;
}
```

#### 4.2.3 配置模型 (Settings)

```typescript
interface Settings {
  editor: {
    fontFamily: string;
    fontSize: number;
    lineHeight: number;
    typewriterMode: boolean;
    typewriterPosition: number;  // 百分比，如 0.5 表示屏幕中央
    zoomLevel: number;           // 缩放级别，1.0 为默认
  };
  ui: {
    theme: 'light' | 'dark' | 'auto';
    layout: 'minimal' | 'three-column';
  };
  git: {
    autoSync: boolean;
    autoSyncInterval: number;    // 分钟
    commitMessageTemplate: string;
  };
  ai: {
    provider: 'openai' | 'claude' | 'custom';
    apiKey?: string;             // 加密存储
    model: string;
  };
}
```

### 4.3 Git 分支策略

**单笔记单分支模型**：

- 分支命名规则：`note/{note-id}`
- 每个笔记独立分支，互不干扰
- 主分支（main/master）用于仓库配置和全局文件

```
main
└── note/abc123 (笔记 A 的分支)
└── note/def456 (笔记 B 的分支)
```

### 4.4 冲突处理策略

1. **检测冲突**：pull 时检测是否有冲突
2. **冲突标识**：在编辑器中用 Git 标准格式标记冲突
3. **可视化解决**：提供三窗口对比（本地、远程、基础版本）
4. **手动解决**：用户编辑后标记为已解决
5. **自动备份**：解决前自动创建备份分支

***

## 5. 关键交互流程说明

### 5.1 笔记创建与编辑流程

```
用户点击新建笔记
    ↓
生成唯一 UUID
    ↓
创建本地笔记文件
    ↓
创建 Git 分支（note/{id}）
    ↓
初始化首次 commit
    ↓
进入编辑界面
    ↓
实时保存（防抖 2s）
    ↓
自动 commit（可选）
    ↓
手动/自动 push
```

### 5.2 GitHub 同步流程

```
用户授权 GitHub
    ↓
选择/创建仓库
    ↓
克隆仓库到本地
    ↓
拉取现有笔记分支
    ↓
[编辑笔记]
    ↓
用户点击同步
    ↓
commit 本地变更
    ↓
pull 远程变更
    ├─ 无冲突 → push
    └─ 有冲突 → 显示冲突解决界面
                  ↓
             用户解决冲突
                  ↓
             commit 解决结果
                  ↓
                  push
```

### 5.3 思维导图同步流程

```
用户编辑 Markdown 列表
    ↓
解析器分析列表结构
    ↓
生成思维导图数据结构
    ↓
更新导图视图
    ↓
[用户在导图中拖拽节点]
    ↓
更新数据结构
    ↓
反向生成 Markdown 列表
    ↓
更新编辑器内容
```

***

## 6. 版本规划

### 6.1 V1.0 版本（MVP）- 必须交付

**平台**：仅 Windows

**核心功能**：

| 模块  | 功能                   | 优先级 |
| --- | -------------------- | --- |
| 界面  | 复刻 Effie 简洁风格        | P0  |
| 编辑器 | 完整 Markdown 语法支持     | P0  |
| 编辑器 | 快捷键支持（第二次按下取消）       | P1  |
| 编辑器 | 打字机模式                | P1  |
| 编辑器 | 全局缩放                 | P1  |
| Git | GitHub OAuth 授权      | P0  |
| Git | 仓库克隆/创建              | P0  |
| Git | 单笔记单分支管理             | P0  |
| Git | Commit / Push / Pull | P0  |
| Git | 基础冲突提示               | P1  |
| 文件  | 本地笔记创建/打开/保存         | P0  |

**交付标准**：

- 可正常创建、编辑、保存 Markdown 笔记
- 可成功连接 GitHub 仓库并同步
- 界面简洁无明显 Bug

### 6.2 V1.1 版本

- 思维导图/大纲视图
- 列表自动生成导图
- 节点折叠/展开
- 视图同步联动

### 6.3 V1.2 版本

- 双向归类机制
- 标签系统
- 表格增强（基础功能）
- 图床集成

### 6.4 V1.3 版本

- AI 插件系统（基础 Chat）
- 上下文感知提问
- 多光标编辑

### 6.5 V2.0 版本

- 跨平台支持（macOS）
- 语音写作（基础转写）
- 高级表格函数
- 插件市场

### 6.6 V2.1 版本

- 移动端支持（Android）
- 语音写作优化版
- 双向同步增强

***

## 7. 非功能性需求与约束

### 7.1 性能要求

- **启动时间**：< 3 秒
- **文件打开**：< 1 秒（10MB 以内）
- **内存占用**：< 200MB（空闲状态）
- **同步延迟**：< 5 秒（网络正常情况下）

### 7.2 安全性要求

- **敏感数据加密**：GitHub Token、API Key 等使用系统密钥加密存储
- **HTTPS 传输**：所有网络请求使用 HTTPS
- **权限最小化**：只请求必要的 GitHub 权限（repo  scope）
- **本地数据隔离**：用户数据存储在用户目录下，权限受限

### 7.3 可靠性要求

- **自动备份**：每 10 分钟自动创建本地备份
- **数据完整性**：Git 保证版本历史可追溯
- **异常恢复**：崩溃后自动恢复未保存的内容
- **离线可用**：无网络时可正常编辑

### 7.4 可扩展性要求

- **插件化架构**：AI、图床等功能通过插件实现
- **模块化设计**：核心与扩展功能分离
- **API 设计**：预留插件开发接口

### 7.5 设计约束

- **极简原则**：UI 元素最少化，隐藏非必要功能
- **Effie 风格**：参考 Effie 的视觉语言和交互模式
- **性能优先**：避免不必要的动画和特效
- **可访问性**：支持键盘操作、屏幕阅读器（未来版本）

***

## 8. 风险与应对

| 风险            | 影响 | 概率 | 应对措施                        |
| ------------- | -- | -- | --------------------------- |
| Electron 包体积大 | 中  | 高  | 采用 ASAR 打包、代码分割、按需加载        |
| 服务端部署复杂度      | 高  | 中  | 采用 Docker 容器化部署，提供标准化镜像     |
| Git 冲突处理复杂    | 中  | 高  | V1.0 先做基础提示，后续版本完善可视化解决     |
| GitHub API 限制 | 中  | 中  | 实现本地缓存，减少 API 调用，使用 Webhook |
| 跨平台适配难度       | 高  | 中  | V1.0 先聚焦 Windows，后续逐步扩展     |
| 后端性能瓶颈        | 中  | 低  | 采用 Redis 缓存、异步处理、MySQL 优化   |

***

## 附录

### A. 参考资料

- [Effie 官方网站](https://www.effie.co/)
- [Electron 文档](https://www.electronjs.org/docs)
- [Vue 3 文档](https://vuejs.org/)
- [Spring Boot 文档](https://spring.io/projects/spring-boot)
- [JGit 文档](https://www.eclipse.org/jgit/)
- [GitHub REST API](https://docs.github.com/en/rest)

### B. 术语表

| 术语       | 说明                                 |
| -------- | ---------------------------------- |
| IPC      | 进程间通信（Inter-Process Communication） |
| MVP      | 最小可行产品（Minimum Viable Product）     |
| OAuth    | 开放授权标准                             |
| WebView2 | Microsoft Edge WebView2 控件         |

***

**文档结束**
